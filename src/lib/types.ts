export type SoundType = {
  id: number;
  name: string;
  imgUrl: string;
  url: string;
};
export type UserInfo = {
  id: string;
  name: string;
  avatar: string;
  email: string;
};
export type ProjectInfo = {
  name: string;
  id: string;
  description: string;
  role: string;
};
